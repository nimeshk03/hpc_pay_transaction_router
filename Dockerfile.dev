FROM rust:1.75-slim
WORKDIR /workspace
RUN apt-get update && apt-get install -y \
    git\
    pkg-config\
    libssl-dev\
    && rm -rf /var/lib/apt/lists/*
RUN rustup component add rustfmt clippy
CMD ["bash"]

